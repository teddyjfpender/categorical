---
import WorkspaceLayout from '../../layouts/WorkspaceLayout.astro';
import { render, getCollection } from 'astro:content';
import { getExercisesByModule } from '../../lib/exercises';

const lessons = await getCollection('lessons');
const lesson = lessons.find(l => l.id === 'category-theory/functors');

if (!lesson) {
  return Astro.redirect('/');
}

const exercises = getExercisesByModule('category-theory');
---

<WorkspaceLayout
  title={lesson.data.title}
  module={lesson.data.module}
  moduleTitle={lesson.data.moduleTitle}
  lessonSlug="functors"
  exercises={exercises}
  lessonContent=""
  description={lesson.data.description}
/>
